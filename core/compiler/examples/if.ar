cell if_test() {
  let x0 = 1.;
  let y0 = float();
  let z = float();
  let w = if y0 < 2. {
    eq(z, 5.);
    let tmp = if y0 < 2. { x0 } else { 2. };
    1.
  } else {
    eq(z, 3.);
    2.
  };
  eq(x0, y0);
}
