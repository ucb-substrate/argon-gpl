cell if_test() {
  let x = 1;
  let y = float();
  let z = float();
  let w = if y < 2 {
    eq(z, 5);
    let tmp = if y < 2 { x! } else { 2 };
    1
  } else {
    eq(z, 3);
    2
  };
  eq(x, y);
  w!;
  z!;
}
